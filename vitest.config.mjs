import { defineConfig, configDefaults } from "vitest/config";

export default defineConfig({
  test: {
    exclude: [...configDefaults.exclude, "**/.claude/worktrees/**"],
    // Дефолтные 5 секунд малы этому проекту (wdbc-lxyl). Три десятка проверок
    // читают содержимое packs-src, а это 6774 файла и ~2.5 секунды чистого
    // ввода-вывода на свободной машине; под параллельной нагрузкой воркеров
    // заметно больше. Падали они по очереди и случайно: красным оказывался
    // каждый раз другой файл, и настоящую поломку в таком прогоне легко
    // проглядеть.
    //
    // Сначала я поднимал таймаут точечно и трижды промахнулся мимо файлов,
    // которые в тот момент просто успели. Гоняться за ними по одному —
    // проигранная игра: любой новый тест по пакам заводит ту же мину.
    //
    // Довод против глобального потолка («спрячет настоящее зависание») при
    // 20 секундах не работает: обычный тест здесь идёт миллисекунды, и
    // зависший всё равно падает — просто позже. Прятать было бы минуту.
    // Самым тяжёлым, что идут по ВСЕМУ packs-src, этого мало, и у них свой
    // PACK_SCAN_TIMEOUT (test/support/pack-docs.mjs).
    testTimeout: 20_000,
  },
});
