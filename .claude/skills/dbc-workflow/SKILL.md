---
name: dbc-workflow
description: Рабочий регламент warhammer-dbc — с чего начинать любую задачу, как проверять сделанное, приём «тест до правки» и проверка мутациями, что считается закрытым шагом. Использовать в начале любой работы по этой системе, особенно при выносе кода в модули, рефакторинге листов и переводе на ApplicationV2.
---

# Как здесь работают

## С чего начинать

`docs/architecture-plan.md` — пошаговый план на 7 этапов, и он же журнал: у каждого
шага записано, что сделано, какие решения приняты и почему. **Читать до того, как
браться за задачу.** Половина вопросов «почему тут так» уже отвечена там.

Справочники рядом: `docs/rules-format.md` (формат правил), `docs/how-to-add-race.md`,
`docs/checks/*.md` (протоколы сверки по этапам).

Статус этапов живёт **только в `docs/architecture-plan.md`** — здесь он не
дублируется: дублированная копия однажды уже разъехалась с планом. Перед опорой
на план сверь его утверждения с фактическим кодом (`module/rules/`,
`module/data/`, тесты): план — он же журнал и может отставать; расхождение
плана с кодом — самостоятельная находка, зафиксируй её отдельным пунктом.

## Три правила на весь план

1. **Новое живёт рядом со старым.** Ни один этап не начинается с удаления работающего
   кода. Сначала новый механизм работает параллельно, потом старый снимается по частям.
2. **Ветка на этап, не на весь план.** Ветвление длиной в месяц не сольётся.
3. **Каждый шаг проверяем.** У каждого шага есть строка «Готово, когда» — проверяемый
   признак. Если признак не выполняется, шаг не закрыт, даже когда код написан.

## Команды

```bash
npm test
```

```bash
npm run lint
```

```bash
npm run packs:build
```

Тестов ~550 в 44 файлах, lint должен быть без ошибок (предупреждения о неиспользуемых
переменных разбирать не входит в план). На пул-реквестах то же самое гоняет CI плюс
проверку круговорота компендиумов.

## Приём выноса кода — он же защита от «переехало, но поехало»

Так сделан весь разбор листа персонажа (7297 → 1159 строк), и так же делается любой
следующий вынос:

1. **Тест пишется ДО правки** и проверяет подсистему через то место, где она работает
   сейчас.
2. Подсистема переезжает в модуль. Функции принимают **актора**, а не лист: лист
   остаётся точкой входа, но в расчёт не входит.
3. В тесте меняется **одна строка** — точка вызова. Ожидания не трогаются, и их
   совпадение до и после есть доказательство, что переезд ничего не поменял.

Вынесенный модуль не импортирует лист обратно. Если тянет — значит, вынесено не то
или не до конца.

## Проверка мутациями

В этом проекте она норма, а не роскошь: намеренно сломать проверяемую логику и
убедиться, что тест падает. Зелёный тест, который не падает от сломанного расчёта,
ничего не проверяет — так однажды выяснилось, что тест подбора элитного архетипа
проходил потому, что подставной актор подходил сразу по двум признакам.

Мутации, которыми проверялся шаг, полезно записать в план рядом с шагом — там уже
десятки таких списков.

## Заглушка Foundry — узкий инструмент

`test/support/foundry-stub.mjs` существует, потому что часть кода наследует классы
Foundry прямо при загрузке модуля. Правило: **как только логика может жить без
Foundry, она переезжает в чистый модуль и проверяется без заглушки.** Большинство
тестов её не импортируют, и так должно остаться.

Заглушка не изображает валидацию и не считает за настоящий движок — проверять через
неё поведение на заведомо неверных данных значит проверять заглушку.

## Проверка руками

Тесты закрывают расчёт. Показ и привязка обработчиков к живой разметке проверяются
только глазами: открыть локальный мир в Foundry (`localhost:30000`), открыть лист,
нажать кнопки. Списки того, что смотрится руками, — в `docs/checks/stage-5-3.md` и
соседних файлах.

Вход для такой проверки — свой мастерский аккаунт в своём локальном мире:
логин, пароль и имя мира у каждого разработчика свои, спросить пользователя,
не подставлять чужие или запомненные откуда-то ранее. Если на `localhost:30000` открывается
экран администратора («There is not currently an active game on this server») —
значит, сервер поднят, а мир ещё не запущен; это делает пользователь, не Claude.
Пароль в поле форму Claude не вводит сама (см. правила безопасности) — либо
просит пользователя ввести его самостоятельно, либо действует через уже открытую
пользователем сессию. Сессия сбрасывается на резком reload/навигации в
браузере-инструменте (проверено на практике) — так что просить о повторном
входе приходится не один раз за сессию.

**Правило (указание пользователя 20.08.2026): любая просьба к пользователю
открыть, перезагрузить мир или как-то самому провзаимодействовать с
Foundry — сразу с кликабельной ссылкой в этом же сообщении, не полагаясь на
то, что она была раньше в переписке.** Не выискивать её выше по чату — дать
заново. Если доступен инструмент предпросмотра браузера (`preview_start` и
подобные) — использовать его, он рисует карточку с кнопкой «Open», как в
интерфейсе пользователя. Если такого инструмента нет — обычная markdown-ссылка,
`[http://localhost:30000](http://localhost:30000)`, не просто текстом/в
бэктиках, чтобы рисовалась кликабельной. Это касается не только просьбы ввести
пароль (с чего правило и возникло), а вообще любого «открой», «перезагрузи»,
«зайди и посмотри».

**Как проверять УЖЕ открытую пользователем сессию, если `computer{screenshot}`
не работает.** В некоторых песочницах браузер-инструмента не компоузит кадр
(«Browser pane is not displayed, not compositing frames», `read_page` отдаёт
пустое дерево, `screen.width/height` читаются как `0`) — это ограничение среды,
не баг Foundry и не повод считать сессию нерабочей. `javascript_tool`
(`action:"javascript_exec"`) при этом работает полностью — им и проверять:

- Читать состояние напрямую через API: `game.settings.get(...)`, `game.actors`,
  `game.items`, `game.time.worldTime`, `game.messages`, `document.querySelector(...)`
  + `.value`/`.textContent`/`outerHTML`.
- Кликать реальные элементы через `el.click()` — работает, даже когда `computer`
  клик по тем же координатам не работает: **обычные кнопки/ссылки внутри листа
  актора `computer` кликает нормально, но кастомные виджеты самого Foundry**
  (вкладки боковой панели, некоторые кнопки контекстного меню в шапке) **клик
  `computer` молча не регистрируют** — при подозрении на «кнопка не работает»
  переключаться на `el.click()` раньше, чем искать баг в обработчике.
- Менять поля форм — **не голым `input.value = x`**, это не проходит на
  React/framework-контролируемых полях (в т.ч. часть форм самого Foundry v14):
  ставить значение через нативный сеттер прототипа и вручную диспатчить событие:
  ```js
  const proto = Object.getPrototypeOf(el);
  Object.getOwnPropertyDescriptor(proto, 'value').set.call(el, val);
  el.dispatchEvent(new Event('input', { bubbles: true }));
  ```
  Голое присвоение `.value` эту цепочку не проходит и обработчик `change`/`input`
  ничего не увидит — выглядит как «поле не реагирует», а на деле не долетело
  событие.
- Подтверждать состояние после действия — не считать клик успешным по факту
  вызова, перечитывать DOM/`game.*` заново (при необходимости — с небольшой
  `setTimeout`-паузой в `Promise`, если ждётся сетевой round-trip до сервера,
  например `game.settings.set`/`game.time.advance`).

Это НЕ обходной путь для входа в мир — вход/пароль по-прежнему делает
пользователь сам (см. абзац выше), эта техника только для работы С уже открытой
им сессией.

**`screen: 0x0`/канвас не инициализируется — первым делом проверить размер
окна браузера-инструмента, а не сразу переходить на обходной путь выше.**
Инцидент 03.09.2026: `live-tester` несколько раз подряд не мог войти в мир
(зависал на экране Join/Setup, `game.ready` не становился `true`), хотя
пользователь подтверждал, что зашёл. Причина оказалась не в логине: окно
браузера-инструмента было 442×411 — Foundry требует минимум 1024×768 и при
меньшем размере показывает собственное блокирующее предупреждение
(«Foundry Virtual Tabletop requires a usable window dimensions... You must
enlarge the size of your window»), из-за чего канвас не инициализируется и
вся остальная диагностика («сессия разлогинена», «сервер не отвечает») ведёт
по ложному следу. Чинится одним вызовом `resize_window` с явными `width`/
`height` (например 1280×900) — **не** `preset:"desktop"` (тот только
сбрасывает эмуляцию, реального размера не гарантирует). Проверять размер
командой `resize_window` (или `screen.width/height` через `javascript_exec`)
ДО того, как тратить время на обходной путь через `javascript_tool` выше —
тот путь остаётся нужен для другого класса поломок («Browser pane is not
displayed, not compositing frames»), а не для этой.

**Живой мир и Chrome общие на всех — параллельная сессия ломает вход или портит
состояние мира.** Один реальный Chrome, один тестовый ГМ-аккаунт, один процесс
Foundry на 30000 порту — если две сессии тестируют одновременно, они либо
выбивают друг друга из логина, либо создают/удаляют тестовые данные в одном
живом мире параллельно (риск того же рода, что уже стоил реальных данных, см.
`safe-bulk-delete-live-data` в общей памяти).

**Отменено 03.09.2026 (в тот же день, что и вводилось): тематическая сессия
снова сама вызывает `live-tester` напрямую, если ей для завершения работы
нужна живая проверка.** Был короткий эксперимент (02.09.2026–03.09.2026 →
03.09.2026), когда прямой вызов заменили обязательным заведением bd-задачи с
меткой `testing` для пакетной команды «Тестирование» — пользователь явно
отменил это тем же днём: «отменяем запрет на лайв-тест и заведение bd на это,
гоняй лайв-тесты в сессии, если не сказано иного».

Порядок по умолчанию: сессия зовёт `live-tester` напрямую, **в фоне**
(`run_in_background`, как у любого агента) — не блокируется в ожидании ответа,
работает дальше или заканчивает ход, реагирует на находки по факту завершения
агента (см. `feedback_live-tester-async` в общей памяти). Сам `live-tester`
координирует лок `wdbc-a4l` (очередь вместо немедленного отказа, бюджет
ожидания — в `.claude/agents/live-tester.md`), тематической сессии вручную
координировать лок не нужно.

Очередь `testing` (`bd create --type=task --label=testing --description=...`)
и команда «Тестирование» (скилл `testirovanie`, `bd recall cmd-testirovanie`)
остаются доступны как ВТОРОЙ путь — не по умолчанию, а когда пользователь явно
просит отложить проверку (например, ночная/автономная сессия без живого
доступа к браузеру, или сознательное решение не ждать сейчас). Если решили
использовать очередь — задача самодостаточна текстом (исполнитель не видит эту
переписку), а для найденных багов действует то же правило: не чинить на
месте, заводить отдельным bd без метки `testing`.

`bd merge-slot` по-прежнему не трогать — привязан к отдельной мердж-автоматике
(«polecats»), назначение которой в этом репозитории не выяснено.

**«Правка не применяется» у Рас/Архетипов/Родных миров/Предсказаний и
подобного (origin-shared) — это почти наверняка НЕ кэш браузера, а неверный
файл.** `raceDef()`/`archetypeEntries()` (`module/apps/race-library.mjs`,
`module/apps/archetypes.mjs`) читают закэшированный КОМПЕНДИУМ
(`game.packs.get(...).getDocuments()`, наполняется в `Hooks.once("ready")`),
а `module/constants/races.mjs`/`archetypes.mjs` — только fallback на случай
пустого кэша. Компендиум собирается из `packs-src/`, а не из `module/
constants/`. Правка `gear`/`skills`/`talents` и т.п. текста Расы/Архетипа
идёт в `packs-src/races|archetypes/.../<файл>.json` (`system.<поле>`), НЕ в
константы — та же ловушка «пак важнее кода», что в `dbc-content`, просто для
этих двух паков легко забыть, поскольку рядом лежит одноимённый JS-файл с
теми же полями. Если правка в `module/constants/*.mjs` в браузере как будто
«иногда применяется, иногда нет» без видимой причины — это и есть симптом:
иногда компендиум ещё не успел наполнить кэш (пустой список документов →
тихий откат на fallback-константы), иногда уже успел (правку не видно).
Проверять так: `await import(".../race-library.mjs"); rl.raceDef("<key>")`
(или `archetypeEntries()["<key>"]`) в консоли — если результат не совпадает с
`module/constants/*.mjs`, искать текст в `packs-src/`, а не гадать про кэш.

**Новое поле в `data/item/*.mjs`/`data/actor/*.mjs` тоже не видно без полной
перезагрузки** — четвёртый отдельный кэш, того же рода, что у `.hbs`.
`CONFIG.Item.dataModels`/`CONFIG.Actor.dataModels` регистрируются ОДИН РАЗ на
`Hooks.once("init")`; cache-busted `import(...+"?t=")` подхватывает свежий код
модуля, но НЕ переустанавливает уже зарегистрированный класс схемы — новое
поле у `actor.createEmbeddedDocuments("Item",[...])` останется `undefined`
(Foundry молча использует старую схему), пока страница не перезагрузится по-настоящему.
Проверено 20.08.2026: добавил `quantity` в `data/item/weapon.mjs`, живой тест
через cache-busted импорт создал предмет без этого поля — юнит-тест
(`item-schemas.test.mjs`, схема + `npm test`) при этом был и остаётся
достаточным доказательством корректности самой схемы, дожидаться нового
релоада специально под это не обязательно.

**Cache-busted импорт модуля с `Hooks.on` вешает ВТОРОЙ обработчик — и
наблюдаемое «задвоение» может быть артефактом проверки, а не багом.**
Проверено 05.09.2026 (wdbc-meh9). Симптом выглядел убедительно: предмет с
записью «Наложить Оглушение» давал на токене ДВЕ иконки вместо одной, данные
на листе при этом верные и одиночные, воспроизводилось три раза подряд.
Причина оказалась в самой проверке: `import(url + "?t=" + Date.now())`
создаёт НОВУЮ область модуля со своей копией модульных переменных и заново
исполняет верхний уровень — а значит, повторно регистрирует хуки. Дальше
обработчиков два, предохранитель у каждого свой (`_syncing` в
`apps/token-conditions.mjs` — модульная переменная), и каждый честно создаёт
свой эффект. После настоящей перезагрузки страницы и работы через уже
загруженные `game.*` API все три повтора дали ровно один эффект.

Отсюда правило: **симптом «сделалось дважды» на живой проверке сперва
перепроверяется на свежем F5 без cache-busted импортов, и только потом
заводится как баг.** Полезная косвенная улика — `Hooks.events.<имя>.length`:
если число растёт от прогона к прогону, обработчики множатся, и виноват
стенд. Правило касается любого модуля, который вешает хуки при загрузке, а не
одних Состояний.

**«Переоткрыть мир» через интерфейс Foundry (Return to Setup → снова войти) —
НЕ то же самое, что настоящая перезагрузка СТРАНИЦЫ браузера, и модульный
кэш может пережить такое «переоткрытие» невредимым.** Проверено 20.08.2026:
после «Return to Setup → войти снова» и cache-busted `import(...+"?t=")`
верхнеуровневого `character-wizard.mjs` метод, добавленный в тот же день,
на инстансе БЫЛ (`typeof app._mechCollectorImmediate === "function"`) — но
ЕГО ЖЕ вложенный статический импорт `import {activateAspirationListeners}
from "../sheets/tabs/aspirations.mjs"` (без своего `?t=`) резолвился в
версию ДО правки этого файла: `fetch()` того же URL с сервера уже отдавал
свежий текст (файл на диске обновлён), а браузерный модульный реестр —
ещё старую скомпилированную версию. Диагностика: сравнить `app.constructor
=== (await import(url+"?fresh="+Date.now())).SomeClass` — `false` выдаёт
несовпадение сразу. Только НАСТОЯЩАЯ перезагрузка страницы (F5/навигация,
не просто переоткрытие мира) гарантированно чистит ВЕСЬ граф ES-модулей,
включая вложенные импорты, до которых cache-busting верхнего уровня не
достаёт. Если правка «есть в файле по `fetch`, но не проявляется в
поведении» — сначала проверить именно это, раньше чем предполагать баг в
логике.

**Правка `.hbs`-шаблона тоже не видна без полной перезагрузки страницы** —
третий, отдельный от двух выше кэш: Foundry компилирует и кэширует Handlebars-
шаблоны один раз при загрузке страницы (`getTemplate`), и повторный
cache-busted `import()` самого JS-модуля (который штатно подхватывает свежий
код) НИКАК на кэш шаблона не влияет — рендер идёт по старой разметке, пока не
случится настоящий `navigate`/F5. Проверено 20.08.2026: правка `.hbs` (новый
`<option>`) была не видна через `import(...+"?t="+Date.now())` в той же
вкладке, но появилась сразу после полного релоада страницы.

**Правка `packs-src/` не долетает до живого мира без пересборки и релоада.**
Порядок: (1) править `packs-src/<пак>/.../<файл>.json`; (2) `npm run
packs:build` — если ругается на «правки в компендиуме, которых нет в
исходниках» (кто-то правил вручную в Foundry и не снял правки), СНАЧАЛА `npm
run packs:unpack` (нужен закрытый мир — попросить пользователя «Return to
Setup» в Foundry, иначе `LEVEL_ITERATOR_NOT_OPEN`), это затрёт свежую правку
текущим состоянием компендиума — переналожить правку ПОСЛЕ unpack, ДО build;
(3) `npm run packs:build` снова; (4) `npm test`; (5) попросить пользователя
**переоткрыть мир** в Foundry — простой `navigate`/новая вкладка
браузера-инструмента НЕДОСТАТОЧНА, нужен настоящий релоад мира, чтобы
`Hooks.once("ready")` заново прогнал `refreshRaceCache`/`refreshArchetypeCache`
по свежесобранному паку.

**С 05.09.2026 `packs:unpack` останавливается и в ОБРАТНОМ случае: когда
база отстаёт от исходников.** Извлечение идёт с `clean:true`, то есть папка
исходника перезаписывается составом базы целиком — документ, которого в базе
нет, из `packs-src` исчезал молча. Прежний сторож смотрел только в одну
сторону («в игре правили после сборки»), и обратный случай однажды стоил 45
удалённых файлов: ветка психосил «Мировой Певец» (44 документа) и Черта
«Аура Смирения» лежали в исходниках, закоммиченные, и отсутствовали в
собранном компендиуме — их туда просто не собирали. Теперь unpack извлекает
во временный каталог, сверяет состав по идентификаторам документов
(`tools/pack-drift.mjs`) и переносит на место, только если терять нечего.
Сообщение называет пак и первые пять потерянных файлов; лечится обычно
`npm run packs:build` (собрать базу из исходников), а `--force` означает
осознанное «эти документы мне не нужны».

**`npm run packs:unpack` сам останавливается перед незакоммиченными правками
packs-src (wdbc-bncx, tools/git-status.mjs) — но это подстраховка на случай
провала памяти, не повод пропускать проверку головой.** `tools/unpack.mjs` не
принимает список паков — он безусловно проходит ВСЕ `LIBRARY_PACKS`/
`JOURNAL_PACKS` разом и перезаписывает `packs-src/<пак>/` содержимым
скомпилированной базы (`extractPack(..., clean:true, ...)`), **даже те паки,
которые в жалобе `packs:build` не упоминались вовсе.** Сообщение об ошибке
«есть правки, которых нет в исходниках: vehicles, bestiary, ...» называет
только паки с реальным дрейфом — но unpack всё равно затрёт и все ОСТАЛЬНЫЕ
паки, включая те, где сейчас лежит чужой несохранённый WIP (рабочая копия
общая с параллельными сессиями, см. выше). Инструмент сам проверяет `git
status --porcelain -- packs-src` и отказывается извлекать, если там есть
незакоммиченное — но словесная версия этого правила (wdbc-3i9g) уже один раз
не сработала, потому что была только текстом, который агент не перечитал
перед вызовом (wdbc-bncx, инцидент 2026-09-03: 40 несохранённых файлов
`packs-src/weapons/**/Рукопашное/**` затёрты без возможности восстановления —
git их не хранил, значит и не мог вернуть). Если сторож остановил unpack и
показал чужие пути — не глушить флагом `--force` не глядя: сначала выяснить у
владельца этих правок (см. `ListAgents`/`SendMessage` — спросить параллельную
сессию, если она жива) или дождаться, пока их закоммитят. `--force` — это
осознанное «эти правки мне не нужны», а не способ обойти лишнее сообщение.

Foundry-сервер — процесс пользователя, не Claude: если `localhost:30000` не
отвечает вовсе (`curl` без ответа), это не повод перезапускать его
самостоятельно — сказать пользователю и подождать. То же для `packs:unpack` —
он сам скажет, если мир занял базы; закрывать мир должен пользователь.

## Чего делать не стоит

- **Не переписывать систему с нуля.** Объём правил здесь больше, чем в большинстве
  коммерческих систем Foundry, и он выверен по книгам.
- **Не решать вариативность через `kind: "script"`** в Конструкторе. Скрипт — аварийный
  выход, см. `dbc-rules`.
- **Не начинать с интерфейса.** 16 681 строка CSS и перевод на ApplicationV2 выглядят
  понятной работой с видимым результатом, но пока правила зашиты в лист, переделка
  интерфейса тащит их за собой и делается дважды. Поэтому этап 6 последний.
- **Не вводить предикат там, где хватает значения.** Другое число у расы — это поле в
  данных расы.
- **Не удалять текст `benefit`.** Машинные правила добавляются рядом с ним.

## Git и трекер

**С 02.09.2026: правки вносятся и ведутся прямо в локальной `main`, не в
отдельном git worktree на сессию.** Отменяет прежний шаг 0 (обязательный
`git worktree add ... origin/main` перед первой правкой, работа только в нём)
— воркдери для повседневной работы больше не заводится. Коммитить в
локальную `main` по ходу работы можно и нужно сразу, точечно (`git add <свои
файлы>`, не `-A`/`git commit -a` — в общей `main` в индексе почти всегда
лежит что-то чужое, см. ниже).

### Финальный этап сессии — автоматический, без подтверждения (с 03.09.2026)

**Коммит, мердж в локальную `main`, гейты, пуш, PR и CI-мониторинг — теперь
последний ШАГ любой сессии, поменявшей код, а не предложение команд в
ожидании отдельного «да».** До 03.09.2026 шаги 7–8 (пуш, `gh pr create`)
только предлагались и ждали подтверждения каждый раз — это отменено прямым
указанием пользователя: «добавь последний этап — автоматически коммит и
мердж ветки (если она есть) в локальный main, CI мониторинг, удаление ветки
или ворктри устаревшей; потом в этом же этапе — автоматический push в
отдельную ветку всех новых правок, CI мониторинг, открытие PR (или
обновление существующего), CI мониторинг, включая правки, если он не
пройден». Не нужно отдельного триггер-слова («закоммить» больше не
обязательно называть) — это делается по факту завершения работы над кодом,
как последний пункт хендофа.

Исполняет обычно агент `git-scribe` (`.claude/agents/git-scribe.md`), вызванный
текущей сессией. У `git-scribe` нет Edit/Write — красный гейт (локальный или
CI) он не чинит сам, а стопорится с точным диагнозом; чинить зовут
`test-doctor`, и `git-scribe` вызывают заново, чтобы продолжить с прерванного
места (он идемпотентен: заново прогоняет гейт, дальше — как обычно). Цикл
«красно → test-doctor → снова git-scribe» — не больше **2** попыток подряд на
один и тот же красный гейт; после второй неудачи — стоп, полный диагноз
пользователю, дальше руками, не пытаться в третий раз втихую.

Локальная часть (у себя на машине, ничего не покидает её):
1. **Коммит точечно.** `git status`/`git diff`, `git add <файлы>` (не `-A`),
   содержательное сообщение по стилю проекта.
2. **Мердж ветки сессии, если она была** (не по умолчанию — обычно правки уже
   прямо в `main`, см. выше; ветка/worktree бывает у отдельно собираемого PR
   или изолированной задачи). Сухая проверка конфликтов (`git merge-tree
   $(git merge-base HEAD origin/main) origin/main <ветка>`) перед
   `git merge --no-ff`.
3. **Гейты на результате** — `npm test`, `npm run lint`, `npm run
   packs:build`. Зелено → шаг 4. Красно → диагностика/фикс-цикл выше.
4. **Удалить смёрдженную ветку/worktree** сразу (`git branch -d`, `git
   worktree remove`) — не дожидаясь пуша или мерджа PR на GitHub.

Удалённая часть (уходит на `origin`):
5. `git fetch origin`, посмотреть, что накопилось непосланным:
   `git log --oneline origin/main..main`.
6. Разбить накопленные коммиты на темы (один тикет/задача — один PR, тот же
   принцип «один блок — один PR», что при разборе бэклога на 21 PR, см.
   `dbc-git-workflow` в памяти).

   **Ветка темы заводится от `origin/main` и набирается cherry-pick'ом своих
   коммитов — не указателем на HEAD локальной `main` (wdbc-34v7, 06.09.2026):**

   ```bash
   git branch pr/<тема> origin/main
   git cherry-pick <свой коммит 1> <свой коммит 2> …
   ```

   Ветка-указатель (`git branch pr/<тема> <hash>`) физически несёт всех
   предков, то есть и чужие темы: ветка есть ссылка на коммит, а не на
   диапазон. Пока это стоило только раздутого дифа (wdbc-lqxq, 2026-09-03: PR
   #350–354 вложились друг в друга, #355 стал суперсетом всех пяти), приём
   считался приемлемым. 06.09.2026 он же дважды за час закрыл ЧУЖИЕ PR (#382,
   #383) и удалил их ветки на remote — потому что шаг «поглощение» ниже
   честно видел чужую работу своим предком. Cherry-pick убирает причину: в
   дифф попадает только твоя тема, и вопрос «чей это коммит» больше не
   возникает.

   Конфликт при cherry-pick (твоя правка опиралась на чужой коммит) —
   разрешать не молча: назвать, какие коммиты не легли. Если правки твои и
   чужие вперемешку в одних строках одного файла и cherry-pick невозможен в
   принципе — временный `git worktree add <path> -b pr/<тема> origin/main`
   (только на время сборки этого PR, не на всю сессию) и точечное наложение
   Edit'ом ТОЛЬКО своих строк поверх `origin/main`-версии; целиком чужой файл
   не копировать.
7. **Перед новой веткой — проверить, не ложится ли тема в уже открытый PR**
   (`gh pr list --state open`): тот же тикет/область, ещё не смёрджен — пушить
   в его ветку (PR обновится сам, отдельного шага не нужно), а не плодить
   дубль. Иначе — новая `pr/<тема>`.
8. Сухая проверка перед пушем (как раньше, шаг 3 старого порядка).
9. `git push origin pr/<тема>` — реально выполняется (никогда `git push
   origin main`, ветка защищена, `GH006`).
10. `gh pr create --base main --head pr/<тема> --title ... --body ...` для
    новой темы — реально выполняется, не просто предлагается. Описание
    честно перечисляет ВСЕ темы, реально вошедшие в диф (`git log
    <origin/main>..HEAD --oneline`), если `main` на момент пуша несёт ещё и
    чужие непринятые коммиты — это допустимый компромисс параллельной работы,
    не ошибка, но должен быть виден в тексте PR.
11. **Проверить остальные открытые PR на поглощение — только через
    `tools/pr-absorbed.mjs` (wdbc-34v7).** Для каждого `gh pr list --state
    open`, кроме только что созданного/обновлённого:

    ```bash
    node tools/pr-absorbed.mjs --branch origin/<старая-ветка> --mine <свои SHA через запятую>
    ```

    Выход `0` — вся работа той ветки твоя, PR закрыть: `gh pr close
    <старый-N> --comment "Поглощён #<новый-N> — тот же диапазон коммитов уже
    входит в него целиком, отдельно мёрджить не нужно."`, его ветку удалить
    (`git push origin --delete <старая-ветка>`). Выход `1` — **не трогать ни
    PR, ни ветку**; вывод называет чужие коммиты поимённо. И то, и другое
    попадает в отчёт (шаг 13), тихих закрытий тут не бывает.

    **`git merge-base --is-ancestor` на этом шаге запрещён.** Он отвечает на
    другой вопрос — «предок ли», — и на общей локальной `main` отвечает «да»
    ВСЕГДА, когда сессий больше одной: любая новая ветка физически включает
    всех предков, в том числе чужие темы. Именно этим 06.09.2026 дважды за
    час были закрыты чужие PR #382 и #383 и удалены их ветки на remote
    (восстанавливали руками). Инструмент спрашивает по-другому: «вся ли
    работа в ветке МОЯ» — сверяя patch-id, а не SHA, поэтому переживает
    cherry-pick с шага 6. `--mine` — SHA коммитов, созданных ЭТОЙ сессией на
    шаге 1; чужие туда не подставлять, чтобы «прошло». Шум трекера
    (`.beads/**`) инструмент считает нейтральным сам, руками сравнивать
    деревья больше не нужно (wdbc-ybob, wdbc-ym75).
12. **CI-мониторинг:** `gh pr checks <N> --watch` (или `gh run watch`, если
    `checks` ещё не проиндексировался) — дождаться результата workflow
    `Check` (`.github/workflows/check.yml`: lint, тесты, круговорот паков).
    Красно → фикс-цикл выше (test-doctor → коммит → пуш той же веткой → снова
    watch), зелено → готово.
13. Отчёт: что закоммичено, что смёрджено локально, какие PR открыты/
    обновлены (ссылки), какие закрыты как поглощённые (шаг 11) и какие
    именно, финальный статус CI по каждому открытому, что не починилось
    (если фикс-цикл исчерпан).

Реальный мердж PR на GitHub по-прежнему делает владелец репозитория
`Batonrain` — роль сессии заканчивается на зелёном, открытом/обновлённом PR,
не на мердже в `main` на GitHub.

`main` на GitHub защищена и принимает только пул-реквесты — прямой
`git push origin main` отбивается `GH006: Protected branch update failed`.

**Рабочая копия и локальная `main` часто общие с параллельными сессиями** —
несколько задач одновременно в одной директории, без разделения по воркдери
это пересекается ещё чаще, чем раньше. Перед `git add`/`git commit`/`git
merge`/`git stash` — смотреть `git status` непосредственно перед операцией,
не по памяти: чужие незакоммиченные файлы не трогать, огулом не стешить
(если стеш всё же нужен — `git stash push -u`, и только после проверки, что
в рабочей копии нет чужого WIP).

Если после чужого коммита `git status`/`git diff` вдруг не показывает СВОИХ
ожидаемых правок в файле — это необязательно потеря: другая сессия могла
закоммитить файл целиком (со своими правками вперемешку с твоими
несохранёнными) под СВОИМ сообщением коммита, никак не упоминающим твою
часть. Проверять через `git log --oneline -3 -- <файл>` и `git show <коммит>
-- <файл>` — если нужный код есть в диффе последнего коммита, всё цело,
просто спрятано под чужим заголовком; коммитить/чинить заново не нужно.
Ужимать до вывода «пропало» стоит только после этой проверки, не раньше.

**Механическая часть git-состояния (`git status`/`git diff --stat`) не
держится в памяти модели до конца хода — Stop-хук `.claude/tools/handoff.py`
детерминированно перезаписывает `.handoff.md` в корне репозитория на каждый
ход.** Полезно, если сессия обрывается до финального сообщения: можно поднять
актуальный git-статус из `.handoff.md`, не полагаясь на память диалога.

Трекер задач — beads (`bd`). bd-issue как механизм ПЕРЕДАЧИ работы другой
сессии не нужен — но заводить `bd`-issue на найденные баги/долги по-прежнему
нормально и поощряется. На машине разработчика `bd` может быть не установлен
(`bd: command not found`) — тогда не подменять его молча markdown-списками,
а сказать об этом.

## Язык

Весь UI, контент, комментарии и документация — на русском. Английские строки в
игровой текст не вносить. Технические идентификаторы (типы, ключи полей, флаги)
остаются как есть.
