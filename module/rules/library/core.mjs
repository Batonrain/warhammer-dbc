// module/rules/library/core.mjs
//
// Правила основной книги — те, что действуют у всех и не зависят ни от расы, ни
// от Происхождения. Источник «core» (rules/sources.mjs) отдаёт их каждому
// актору, а отбор идёт по условию `when`.
//
// Здесь живут правила, которые нельзя приписать одной расе: Черта есть у
// нескольких, а действует она на того, кто атакует её носителя.

export const CORE_RULES = [
  {
    // «Nimble (X) / Проворный» (книга, статья о Черте плюс сводная таблица
    // Черт): «Все атаки по нему получают штраф −Х» — флэт от РЕЙТИНГА самой
    // Черты, а не от Бонуса Ловкости цели (никакой Ag.b книга тут не
    // упоминает — старая версия правила путала штраф с другим источником).
    // Черта записана у Астартес, Азуриан, Друкхари и Кроорка, поэтому
    // правило не принадлежит расе: оно отбирается по Черте у ЦЕЛИ.
    //
    // Числа в данных быть не может — Рейтинг X хранится не в правиле, а на
    // самом предмете Черты (system.rating), и может набегать сразу с
    // нескольких грантеров («Nimble (10)» от Геносемени + «Nimble (10)» от
    // импланта «Защитные Рефлексы» — book/core.json). Поэтому `valueFrom`
    // вместо `value`; `targetTraitRating` складывает Рейтинг всех одноимённых
    // Черт цели тем же способом, что и показ строки на листе
    // (merge-abilities.mjs).
    //
    // Рейтинг в названии Черты у разных рас записан по-разному («Nimble» и
    // «Nimble (10)»), а сравнение имён отбрасывает скобки на конце
    // (rules/predicates.mjs), поэтому в условии достаточно короткого имени.
    //
    // Книга снимает штраф, если цель Оглушена или Беспомощна — это штраф за
    // скорость цели, а обездвиженная цель ей не пользуется (CONDITIONS_DEF.
    // stunned/helpless). Ступор считается Оглушением «для прочих эффектов»
    // (стр. 30-31, wdbc-r5o7.3) — тоже гасит штраф.
    //
    // Имплант «Чёрный Панцирь / Black Carapace» (DoomBC — ГЕНОСЕМЯ) прямо
    // пишет: «позволяет сохранять Трейт Nimble в силовой броне» — то есть без
    // него силовая броня штраф гасит целиком (targetKeepsNimbleInArmour).
    id: "core.nimble",
    label: "Проворный (цель уворачивается)",
    when: {
      targetHasTrait: "Nimble",
      targetLacksCondition: ["stunned", "dazed", "helpless"],
      targetKeepsNimbleInArmour: true
    },
    effects: [{
      kind: "rollBonus",
      target: "attack",
      valueFrom: { targetTraitRating: "Nimble", multiplier: -1 }
    }]
  },
  {
    // Миазмы (мутация, core.json, «73 Миазмы»; wdbc-1rno.11): «…дает бонус
    // +40 на попытки отследить его по запаху» — бонус получает ТОТ, КТО
    // выслеживает носителя, а не сам мутант (прежняя запись на предмете была
    // смоделирована наоборот). Правило со стороны бросающего по ЦЕЛИ, как
    // Nimble. Галочка, а не авто: «по запаху» — один из способов выслеживать.
    id: "core.miasmaTracking",
    label: "Цель с Миазмами: выслеживание по запаху",
    when: { targetHasTrait: "Miasma", targetLacksSealedArmour: true },
    effects: [{ kind: "rollBonus", target: "skill:survival", value: 40 }]
  },
  {
    // Подавляющее поле друкхарийской брони (Психокостяной Тканый Костюм −10,
    // Призрачная Броня −20; constants/drukhari-armor-fields.mjs, wdbc-j8cn):
    // «вражеские психотесты против него получают −X». Как и Nimble — правило
    // со стороны бросающего, отобранное по состоянию ЦЕЛИ. Галочка, а не
    // auto: «вражеские» движок не различает — союзный псайкер, вливающий в
    // друкхари силу, штраф не получает, и решает это игрок.
    id: "core.drukhariFieldPsyMod",
    label: "Подавляющее поле цели",
    when: { targetHasFieldPsyMod: true },
    effects: [{
      kind: "rollBonus",
      target: "power",
      valueFrom: { targetFieldPsyMod: true }
    }]
  },
  {
    // Таблица Размера (стр. 30): каждая ступень Размера цели — ±10 к попаданию
    // по ней атакующим, знак совпадает со знаком Размера. Гейт targetHasSize
    // не даёт строке с «(+0)» лезть в чек-лист против обычного человека.
    id: "core.sizeToHit",
    label: "Размер цели",
    when: { targetHasSize: true },
    effects: [{
      kind: "rollBonus",
      target: "attack",
      valueFrom: { targetSize: true, multiplier: 10 }
    }]
  },
  {
    // Та же таблица, обратный столбец: Размер даёт МИНУС к собственным тестам
    // Скрытности (крупнее — заметнее), знак противоположен столбцу попадания.
    id: "core.sizeStealth",
    label: "Размер (Скрытность)",
    when: { hasSize: true },
    effects: [{
      kind: "rollBonus",
      target: "skill:stealth",
      valueFrom: { selfSize: true, multiplier: -10 }
    }]
  },
  {
    // «Доступна папка талантов Псайкера/Псайканы» (item-picker.mjs::
    // talentGroupLock, wdbc-sauo) раньше читала Пси-Рейтинг актора напрямую —
    // не по расе (любая раса с Пси-Рейтингом отпирает обе папки одинаково),
    // поэтому правило общее, не расовое.
    id: "core.psyker",
    label: "Пси-Рейтинг",
    when: { psyRatingMin: 1 },
    effects: [{ kind: "grantFlag", target: "talents.psyker" }]
  },
  {
    // Avatar of Slaughter/Аватар Резни (Черта, элитный архетип Берсерк
    // Кхорна, wdbc-sk8s) — раньше жила своим отдельным источником
    // "avatarOfSlaughter" (rules/sources.mjs), хотя когда-то была
    // зарегистрирована совсем как "core" — статичное when, читающее метку
    // на самом акторе (rules/predicates.mjs::avatarOfSlaughterOffTarget),
    // без cross-actor обхода. Перенесена сюда (wdbc-shr, находка 1): нет
    // причины заводить отдельный источник ради одного статического правила,
    // когда основная книга уже отдаёт такие через "core" оптом. Покрывает
    // только «атаки» (ctx.kind==="attack") — «манёвры» (Состязания,
    // combat/techniques.mjs) используют другой вид теста, не покрытый
    // effectAppliesTo, оставлено открытым пробелом.
    id: "avatarOfSlaughter.penalty",
    label: "Аватар Резни: не тот противник",
    when: { avatarOfSlaughterOffTarget: true },
    effects: [{ kind: "rollBonus", target: "attack", value: -20, label: "Аватар Резни: атакует не Берсерка" }]
  }
];
