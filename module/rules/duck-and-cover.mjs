// module/rules/duck-and-cover.mjs
// ════════════════════════════════════════════════════════════════════════
//  Перебежка (стр. 30, wdbc-x1nz.2.38): «до конца Раунда может перебрасывать
//  тесты Избегания и Подавления» — флаг ставит combat/movement-actions.mjs::
//  declareDuckAndCover, гасится тем же тактом, что running/exposedAggressive
//  (turn-flags.mjs — «до начала следующего своего Хода», то же приближение
//  книжного «до конца Раунда», что уже используется этими соседями).
// ════════════════════════════════════════════════════════════════════════

/** Преимущество на ЭТО Уклонение/Парирование/Подавление — Перебежка активна. */
export function duckAndCoverAdvantage(actor) {
  return !!actor?.getFlag?.("warhammer-dbc", "duckAndCoverActive");
}
